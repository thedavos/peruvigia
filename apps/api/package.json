{
  "name": "@peruvigia/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "vp pack src/server.ts -f esm -d dist",
    "check": "vp check package.json tsconfig.json src"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/swagger": "^9.5.1",
    "@fastify/swagger-ui": "^5.2.2",
    "@peruvigia/shared": "workspace:*",
    "fastify": "^5.6.1"
  }
}
